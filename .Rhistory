# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
head(coldata, 5)
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata)==colnames(data)) # Ensure they are in the right order
# first create the design formula
# Now create the DESeq2 Dataset`
#condition <- NC_1 + NC_3 + NC_ind_1 + NC_ind_3 + s4_1 + s4_3 + s4_ind_1 + s4_ind_3 + WT_1 + #WT_3 + WT_ind_1 + WT_ind_3
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
dds <- DESeq(dds)
res <- results(dds)
plotMA(res, ylim=c(-10,10))
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()
quant_fp <- paste(cwd, 'quants/', sep = '/')
quant_files <- list.files(path = quant_fp, pattern="quant.sf$", recursive = TRUE)
# And we can then extract the sample names.
quant_dirs <- list.files(quant_fp, pattern = "_quant$", full.names = TRUE)
sample_names <- gsub("_quant$", "", basename(quant_dirs))
# correct for the FASTQ generation file included in the directory.
sample_names <- sample_names[1: 48]
# And now we can create the txi import, note that it can take an array of file names.
txi <- tximport(paste(cwd, "quants", quant_files, sep='/'), type = "salmon", txOut = TRUE)
# Here we will create a temporary Dataframe for the TXI data so that we can add the sample names as columns.
# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
head(coldata, 5)
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata)==colnames(data)) # Ensure they are in the right order
# first create the design formula
# Now create the DESeq2 Dataset`
#condition <- NC_1 + NC_3 + NC_ind_1 + NC_ind_3 + s4_1 + s4_3 + s4_ind_1 + s4_ind_3 + WT_1 + #WT_3 + WT_ind_1 + WT_ind_3
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
dds <- DESeq(dds)
res <- results(dds)
plotMA(res, ylim=c(-10,10))
```{r}
```{r}
```{r}
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()
quant_fp <- paste(cwd, 'quants/', sep = '/')
quant_files <- list.files(path = quant_fp, pattern="quant.sf$", recursive = TRUE)
# And we can then extract the sample names.
quant_dirs <- list.files(quant_fp, pattern = "_quant$", full.names = TRUE)
sample_names <- gsub("_quant$", "", basename(quant_dirs))
# correct for the FASTQ generation file included in the directory.
sample_names <- sample_names[1: 48]
# And now we can create the txi import, note that it can take an array of file names.
txi <- tximport(paste(cwd, "quants", quant_files, sep='/'), type = "salmon", txOut = TRUE)
# Here we will create a temporary Dataframe for the TXI data so that we can add the sample names as columns.
# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
head(coldata, 5)
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata)==colnames(data)) # Ensure they are in the right order
# first create the design formula
# Now create the DESeq2 Dataset`
#condition <- NC_1 + NC_3 + NC_ind_1 + NC_ind_3 + s4_1 + s4_3 + s4_ind_1 + s4_ind_3 + WT_1 + #WT_3 + WT_ind_1 + WT_ind_3
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
dds <- DESeq(dds)
res <- results(dds)
plotMA(res, ylim=c(-10,10))
print(res)
significant_res <- subset(res < 0.05)
significant_res <- subset(res < 0.05)
significant_res <- subset(res > 0.05)
significant_res <- subset(res > 0.1)
significant_res <- subset(res > 5)
significant_res <- subset(res > 10)
significant_res <- subset(res > 10)
significant_res <- subset(res < 10)
head(res)
significant_res <- subset(res.pvalue < 10)
significant_res <- subset(res[pvalue,]< 10)
significant_res <- subset(res["pvalue",]< 10)
significant_res <- subset(res["pvalue",]<0.05)
significant_res <- subset(res["pvalue",]>0.05)
head(res["pvalue",
])
print(res["pvalue",])
print(res[["pvalue",]])
significant_res <- subset(res[["pvalue",]]>0.05)
significant_res <- subset(res[["pvalue",]]<0.05)
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()
quant_fp <- paste(cwd, 'quants/', sep = '/')
quant_files <- list.files(path = quant_fp, pattern="quant.sf$", recursive = TRUE)
# And we can then extract the sample names.
quant_dirs <- list.files(quant_fp, pattern = "_quant$", full.names = TRUE)
sample_names <- gsub("_quant$", "", basename(quant_dirs))
# correct for the FASTQ generation file included in the directory.
sample_names <- sample_names[1: 48]
# And now we can create the txi import, note that it can take an array of file names.
txi <- tximport(paste(cwd, "quants", quant_files, sep='/'), type = "salmon", txOut = TRUE)
# Here we will create a temporary Dataframe for the TXI data so that we can add the sample names as columns.
# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
head(coldata, 5)
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata)==colnames(data)) # Ensure they are in the right order
# first create the design formula
# Now create the DESeq2 Dataset`
#condition <- NC_1 + NC_3 + NC_ind_1 + NC_ind_3 + s4_1 + s4_3 + s4_ind_1 + s4_ind_3 + WT_1 + #WT_3 + WT_ind_1 + WT_ind_3
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
dds <- DESeq(dds)
res <- results(dds)
plotMA(res, ylim=c(-10,10))
print(res[["pvalue",]])
significant_res <- subset(res[["pvalue",]]<0.05)
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()
quant_fp <- paste(cwd, 'quants/', sep = '/')
quant_files <- list.files(path = quant_fp, pattern="quant.sf$", recursive = TRUE)
# And we can then extract the sample names.
quant_dirs <- list.files(quant_fp, pattern = "_quant$", full.names = TRUE)
sample_names <- gsub("_quant$", "", basename(quant_dirs))
# correct for the FASTQ generation file included in the directory.
sample_names <- sample_names[1: 48]
# And now we can create the txi import, note that it can take an array of file names.
txi <- tximport(paste(cwd, "quants", quant_files, sep='/'), type = "salmon", txOut = TRUE)
# Here we will create a temporary Dataframe for the TXI data so that we can add the sample names as columns.
# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
head(coldata, 5)
# first create the design formula
# Now create the DESeq2 Dataset`
#condition <- NC_1 + NC_3 + NC_ind_1 + NC_ind_3 + s4_1 + s4_3 + s4_ind_1 + s4_ind_3 + WT_1 + #WT_3 + WT_ind_1 + WT_ind_3
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
dds <- DESeq(dds)
print(res[["pvalue",]])
significant_res <- subset(res[["pvalue",]]<0.05)
significant_res <- subset(res[["pvalue",]]<0.05)
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()
quant_fp <- paste(cwd, 'quants/', sep = '/')
quant_files <- list.files(path = quant_fp, pattern="quant.sf$", recursive = TRUE)
# And we can then extract the sample names.
quant_dirs <- list.files(quant_fp, pattern = "_quant$", full.names = TRUE)
sample_names <- gsub("_quant$", "", basename(quant_dirs))
# correct for the FASTQ generation file included in the directory.
sample_names <- sample_names[1: 48]
# And now we can create the txi import, note that it can take an array of file names.
txi <- tximport(paste(cwd, "quants", quant_files, sep='/'), type = "salmon", txOut = TRUE)
# Here we will create a temporary Dataframe for the TXI data so that we can add the sample names as columns.
# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
head(coldata, 5)
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata)==colnames(data)) # Ensure they are in the right order
# first create the design formula
# Now create the DESeq2 Dataset`
#condition <- NC_1 + NC_3 + NC_ind_1 + NC_ind_3 + s4_1 + s4_3 + s4_ind_1 + s4_ind_3 + WT_1 + #WT_3 + WT_ind_1 + WT_ind_3
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
dds <- DESeq(dds)
res <- results(dds)
plotMA(res, ylim=c(-10,10))
print(res[["pvalue",]])
significant_res <- subset(res[["pvalue",]]<0.05)
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()d
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()denoch
# First lets list all of the quant.sf files
cwd <- getwd()
quant_fp <- paste(cwd, 'quants/', sep = '/')
quant_files <- list.files(path = quant_fp, pattern="quant.sf$", recursive = TRUE)
# And we can then extract the sample names.
quant_dirs <- list.files(quant_fp, pattern = "_quant$", full.names = TRUE)
sample_names <- gsub("_quant$", "", basename(quant_dirs))
# correct for the FASTQ generation file included in the directory.
sample_names <- sample_names[1: 48]
# And now we can create the txi import, note that it can take an array of file names.
txi <- tximport(paste(cwd, "quants", quant_files, sep='/'), type = "salmon", txOut = TRUE)
# Here we will create a temporary Dataframe for the TXI data so that we can add the sample names as columns.
# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
head(coldata, 5)
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata)==colnames(data)) # Ensure they are in the right order
# first create the design formula
# Now create the DESeq2 Dataset`
#condition <- NC_1 + NC_3 + NC_ind_1 + NC_ind_3 + s4_1 + s4_3 + s4_ind_1 + s4_ind_3 + WT_1 + #WT_3 + WT_ind_1 + WT_ind_3
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
res <- results(dds)
plotMA(res, ylim=c(-10,10))
print(res[["pvalue",]])
print(res)
print(res)
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()
quant_fp <- paste(cwd, 'quants/', sep = '/')
quant_files <- list.files(path = quant_fp, pattern="quant.sf$", recursive = TRUE)
# And we can then extract the sample names.
quant_dirs <- list.files(quant_fp, pattern = "_quant$", full.names = TRUE)
sample_names <- gsub("_quant$", "", basename(quant_dirs))
# correct for the FASTQ generation file included in the directory.
sample_names <- sample_names[1: 48]
# And now we can create the txi import, note that it can take an array of file names.
txi <- tximport(paste(cwd, "quants", quant_files, sep='/'), type = "salmon", txOut = TRUE)
# Here we will create a temporary Dataframe for the TXI data so that we can add the sample names as columns.
# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
head(coldata, 5)
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata)==colnames(data)) # Ensure they are in the right order
# first create the design formula
# Now create the DESeq2 Dataset`
#condition <- NC_1 + NC_3 + NC_ind_1 + NC_ind_3 + s4_1 + s4_3 + s4_ind_1 + s4_ind_3 + WT_1 + #WT_3 + WT_ind_1 + WT_ind_3
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
dds <- DESeq(dds)
res <- results(dds)
plotMA(res, ylim=c(-10,10))
print(res[["pvalue",]])
significant_res <- subset(res[["pvalue",]]<0.05)
dds <- DESeq(dds)
dds <- DESeq(dds)
print(res)
contrast.matrix <- makeContrasts(
A_vs_B = conditionB - conditionA,
A_vs_C = conditionC - conditionA,
B_vs_C = conditionC - conditionB,
levels = colData(dds)$condition
)
contrast.matrix <- makeContrasts(
NC1_vs_NC3 = NC_1  - NC_3,
NC1_vs_NC_ind_1 = NC_1 - NC_ind_3_ind_1,
NC1_vs_WT_1 = NC_1 - WT_1
levels = colData(dds)$condition
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
coldata
res <- results(dds)
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()
quant_fp <- paste(cwd, 'quants/', sep = '/')
quant_files <- list.files(path = quant_fp, pattern="quant.sf$", recursive = TRUE)
# And we can then extract the sample names.
quant_dirs <- list.files(quant_fp, pattern = "_quant$", full.names = TRUE)
sample_names <- gsub("_quant$", "", basename(quant_dirs))
# correct for the FASTQ generation file included in the directory.
sample_names <- sample_names[1: 48]
# And now we can create the txi import, note that it can take an array of file names.
txi <- tximport(paste(cwd, "quants", quant_files, sep='/'), type = "salmon", txOut = TRUE)
# Here we will create a temporary Dataframe for the TXI data so that we can add the sample names as columns.
# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
coldata
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata)==colnames(data)) # Ensure they are in the right order
# first create the design formula
# Now create the DESeq2 Dataset`
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
dds <- DESeq(dds)
res <- results(dds)
plotMA(res, ylim=c(-10,10))
resOrdered <- res[order(res$pvalue),]
write.csv(as.data.frame(resOrdered), "datacsv.csv")
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()
quant_fp <- paste(cwd, 'quants/', sep = '/')
quant_files <- list.files(path = quant_fp, pattern="quant.sf$", recursive = TRUE)
# And we can then extract the sample names.
quant_dirs <- list.files(quant_fp, pattern = "_quant$", full.names = TRUE)
sample_names <- gsub("_quant$", "", basename(quant_dirs))
# correct for the FASTQ generation file included in the directory.
sample_names <- sample_names[1: 48]
# And now we can create the txi import, note that it can take an array of file names.
txi <- tximport(paste(cwd, "quants", quant_files, sep='/'), type = "salmon", txOut = TRUE)
# Here we will create a temporary Dataframe for the TXI data so that we can add the sample names as columns.
# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
print(coldata)
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata)==colnames(data)) # Ensure they are in the right order
# first create the design formula
# Now create the DESeq2 Dataset`
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
dds <- DESeq(dds)
res <- results(dds)
plotMA(res, ylim=c(-10,10))
print(
library("tximport")
library("readr")
library("DESeq2")
library(data.table)
# First lets list all of the quant.sf files
cwd <- getwd()
quant_fp <- paste(cwd, 'quants/', sep = '/')
quant_files <- list.files(path = quant_fp, pattern="quant.sf$", recursive = TRUE)
# And we can then extract the sample names.
quant_dirs <- list.files(quant_fp, pattern = "_quant$", full.names = TRUE)
sample_names <- gsub("_quant$", "", basename(quant_dirs))
# correct for the FASTQ generation file included in the directory.
sample_names <- sample_names[1: 48]
# And now we can create the txi import, note that it can take an array of file names.
txi <- tximport(paste(cwd, "quants", quant_files, sep='/'), type = "salmon", txOut = TRUE)
# Here we will create a temporary Dataframe for the TXI data so that we can add the sample names as columns.
# Before feeding to DESeq2 we need to convert back to a numeric matrix.
atts <- attributes(txi)
data <- data.table(data = txi$counts) # Using DaRMDtaTables allows for better performance
name_list <- attributes(txi)
colnames(data) <- sample_names
# Extract the conditions from the file names supplied by Jen
#temp <- sub(pattern ="ATEY090523-", "",sample_names)
# Set experimental conditons
type <- "single-read" # Set Read conditions
condition <- sub(pattern ="_L.*", "", sample_names)
coldata <- data.frame(condition, type)
rownames(coldata) <- sample_names
# According to the DESeq2 vignette the above code results in a conditions matrix for which the ordering is not the same as the samples.
# It is important to resolve this issue
# This does not seem to be true for our data but its good to make sure.
# We also need to make sure that the coldata labels match the counts data.
print(coldata)
all(rownames(coldata) %in% colnames(data)) # Ensure that all samples are present in both the data and coldata.
all(rownames(coldata)==colnames(data)) # Ensure they are in the right order
# first create the design formula
# Now create the DESeq2 Dataset`
dds = DESeqDataSetFromMatrix(round(data), colData = coldata, design = ~ condition)
dds <- DESeq(dds)
res <- results(dds)
plotMA(res, ylim=c(-10,10))
print(
print(
